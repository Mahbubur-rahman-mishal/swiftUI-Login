# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FireBase SwiftUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'Firebase/Auth'

  # Pods for FireBase SwiftUI

  target 'FireBase SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FireBase SwiftUIUITests' do
    # Pods for testing
  end

end
